%h1 Listing authors

- if @authors
  %table.table.table-striped
    %tr
      %th Author
      %th
      %th
      %th

    - @authors.each do |author|
      %tr
        %td= author.name
        %td= link_to 'Show', [:admin, author]
        %td= link_to 'Edit', edit_admin_author_path(author)
        %td= link_to 'Destroy', [:admin, author], method: :delete, data: { confirm: 'Are you sure?' }
- else
  .alert.alert-block
    %h4 Empty list
    %p You need to add some authors or books

%br

= link_to content_tag(:i, '', class: 'icon-plus') + ' New Author', new_admin_author_path, class: 'btn'
